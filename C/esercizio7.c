#include <stdio.h>

int main() {
    int numero1 = 10;   // Prima variabile
    int numero2 = 5;    // Seconda variabile
    int risultato;      // Variabile per il risultato

    risultato = numero1 + numero2;  // Operazione aritmetica (somma)

    printf("La somma di %d e %d è: %d\n", numero1, numero2, risultato);

    return 0;
}
