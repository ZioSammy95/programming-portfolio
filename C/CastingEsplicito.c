#include <stdio.h>

int main() {
    int a = 10, b = 3;
    float risultato = (float)a / b;  // 'a' viene convertito in float prima della divisione

    printf("Risultato: %f\n", risultato);
    return 0;
}
