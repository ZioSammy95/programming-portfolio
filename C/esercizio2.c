#include <stdio.h>

int main() {
    int numero = 5;  // Dichiarazione e inizializzazione
    printf("Valore iniziale di numero: %d\n", numero);
    
    numero = 10;  // Modifica del valore della variabile
    printf("Nuovo valore di numero: %d\n", numero);
    
    return 0;
}
