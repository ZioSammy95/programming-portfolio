#include <stdio.h>

int main() {
    int a = 10;
    float b = 2.5;
    float risultato = a + b;  // 'a' (int) viene convertito automaticamente in float

    printf("Risultato: %f\n", risultato);
    return 0;
}
