#include <stdio.h>
#include <stdbool.h>  // Includi l'header per usare il tipo bool

int main() {
    bool vero = true;  // Assegna il valore "vero" alla variabile

    // Stampa del valore della variabile
    printf("La variabile 'vero' è: %d\n", vero);  // 'vero' sarà 1 (true)

    return 0;
}